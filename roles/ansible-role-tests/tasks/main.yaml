- name: Install latest ansible
  command: pip install ansible
  become: yes

- name: Run ansible-role-tests
  command: 'ansible-playbook --inventory ./inventory test.yml'
  args:
    chdir: "{{ ansible_network_engine_path }}/tests/"
