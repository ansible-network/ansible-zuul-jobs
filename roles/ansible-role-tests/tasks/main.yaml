- name: Install Ansible
  pip:
    name: ansible
    version: "2.5.0"
 
- name: Install textfsm
  pip:
    name: textfsm
    state: present

- name: Run ansible-role-tests for network-engine
  command: "{{ item }}"
  args:
    chdir: "{{ ansible_network_engine_path }}/tests/"
  with_items:
    - 'ls -l'
    - 'ansible-playbook --inventory ./inventory test.yml -vv'
